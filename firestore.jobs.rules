rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public job reads are served by the server API. Firebase Admin bypasses these
    // rules, so no browser client can create, edit, or delete a job directly.
    match /jobs/{jobId} {
      allow read, write: if false;
    }
  }
}
