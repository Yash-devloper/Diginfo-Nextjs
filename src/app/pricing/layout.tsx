export const metadata = {
  title: "Pricing | Diginfo Agency",
  description: "View our pricing plans and packages.",
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}